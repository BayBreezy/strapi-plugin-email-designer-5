/**
 * Plugin identifier and name constants for the Email Designer plugin
 *
 * This module exports the plugin identifiers used throughout the admin UI
 * for accessing the Email Designer plugin's services, routes, and configuration.
 */

/**
 * Unique identifier for the Email Designer plugin
 * Used in Strapi admin UI routing and component identification
 */
export const PLUGIN_ID = "email-designer-5";

/**
 * Plugin name used for service access and API endpoints
 * Used when accessing plugin services: strapi.plugin(pluginName).service(...)
 */
export const pluginName = "email-designer-5";
