import configController from "./config";
import designerController from "./designer";
import versionController from "./version";

export default {
  config: configController,
  designer: designerController,
  version: versionController,
};
